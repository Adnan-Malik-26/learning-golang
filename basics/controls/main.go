package main


func main(){
	/*
	if condition {
		code that is to be run if the condition is true
		condiditon is a boolean argument
	}

	if condition {
		if condiditon is true this codeblock will run
	} else {
		if the condidtion is false this code block will run	
	}
	*/

	/*

	switch expression {
	case condition1:
		if expression==condition1 then this block will run
	case condition2:
		if expression==condition2 then this block will run
	.
	.
	.
	default:
	if none of the conditions are true then this block will run
	}
	*/
}
